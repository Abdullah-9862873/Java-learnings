package pkg3_nestedinterface;


public class A {
    public interface NestedInterface{
        boolean isOdd(int num);
    }
}

/*
In case of Nested interface the top level class has to be declared as public or the default one... But the nested interface can be declared as public protected or private
 */

